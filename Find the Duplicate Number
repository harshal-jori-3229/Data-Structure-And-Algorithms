var findDuplicate = function(nums) {
    let map = new Map();
    for(let num of nums){
        if(map.has(num)){
            return num;
        }

        map.set(num, (map.get(num)||0)+1);
    }
    return false;
};
