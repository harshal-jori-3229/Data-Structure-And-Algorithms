var findGCD = function(nums) {
    
    let min = Math.min(...nums);
    let max = Math.max(...nums);
    
    while(min !== 0){
        let temp = min;
        min = max % min;
        max = temp;
    }
    return max;
};

let nums = [2,5,6,9,10];
console.log(findGCD(nums));
