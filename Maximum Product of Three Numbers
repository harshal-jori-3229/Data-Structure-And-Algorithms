var maximumProduct = function(nums) {
    let n = nums.length;

    nums.sort((a,b)=> a - b);

    let option1 = nums[n-1] * nums[n-2] * nums[n-3];

    let option2 = nums[0] * nums[1] * nums[n-1];

    let ans = Math.max(option1, option2);

    return ans;
};
